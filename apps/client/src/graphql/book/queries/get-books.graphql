query GetBooks($query: BookQueryInput!) {
  getBooks(query: $query) {
    items {
      id
      title
      language
      rating
      url
      favoriteCategory
      author {
        id
        name
      }
    }
    count
  }
}
